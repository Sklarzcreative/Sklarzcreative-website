"""Deterministic vector builders for the VECTOR_BUILD route.

These produce the figure's scientific content from a VERIFIED SOURCE, not from
a model's memory. Same input always yields the same drawing, and every drawing
carries the citation it was built from.
"""
from . import chem, diagram  # noqa: F401
