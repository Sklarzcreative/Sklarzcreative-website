# Client-confidential material. This repository is PUBLIC (serves sklarzcreative.com).
# The Cannabiology manuscript, figure prompts, tracker data, generated artwork and
# review packets are unpublished client IP and must never be committed here.
# Canonical home for all of it is Google Drive; this directory is a working copy.
client-data/

# Local config may hold provider settings; keys come from the environment.
pipeline/config.json

# Python
__pycache__/
*.pyc
